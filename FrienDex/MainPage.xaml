<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:FrienDex.Models"
             x:Class="FrienDex.MainPage"
             BackgroundColor="{DynamicResource Light}">

    <ScrollView>

        <!-- Only one direct child is allowed inside ScrollView. 
     Wrap both Grid and VerticalStackLayout in a parent layout, e.g., VerticalStackLayout. -->
        <VerticalStackLayout Spacing="0" Padding="0">
            <Grid Padding="5" RowSpacing="5" ColumnSpacing="5"
                RowDefinitions="Auto,Auto,Auto,Auto,*">

                <Entry x:Name="newTestItem"
                    Placeholder="Enter name" />

                <Button Text="Add TestItem"
                    Grid.Row="1"
                    Clicked="OnNewButtonClicked" />

                <Label x:Name="statusMessage"
                    TextColor="{StaticResource Primary}"
                    Grid.Row="2" />

                <Button Text="Get All Test Items"
                    Grid.Row="3"
                    Clicked="OnGetButtonClicked" />


                <CollectionView x:Name="testItemsList" Grid.Row="4">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="model:TestItem">
                            <Grid ColumnDefinitions="*,2*">
                                <Label Text="{Binding Id}" TextColor="{StaticResource Primary}"/>
                                <Label Grid.Column="1" Text="{Binding Name}" TextColor="{StaticResource Primary}"/>
                            </Grid>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>



            <VerticalStackLayout
                Padding="30,0"
                Spacing="25">
                <Image
                    Source="dotnet_bot.png"
                    HeightRequest="185"
                    Aspect="AspectFit"
                    SemanticProperties.Description="dot net bot in a submarine number ten" />

                <Label
                    Text="Hello, World!"
                    Style="{StaticResource Headline}"
                    SemanticProperties.HeadingLevel="Level1" />

                <Label
                    Text="Welcome to &#10;.NET Multi-platform App UI"
                    Style="{StaticResource SubHeadline}"
                    SemanticProperties.HeadingLevel="Level2"
                    SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

                <Button
                    x:Name="CounterBtn"
                    Text="Click me" 
                    SemanticProperties.Hint="Counts the number of times you click"
                    Clicked="OnCounterClicked"
                    HorizontalOptions="Fill" />
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
